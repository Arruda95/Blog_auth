<%= form_with(model: [ @post, @post.comments.build ]) do |form| %>
    <div class="form-group">
      <%= form.text_area :content, rows: 3, placeholder: "Adicione um comentário...", class: "form-control" %>
    </div>
    <%= form.submit "Comentar", class: "btn btn-primary" %>
  <% end %>
  