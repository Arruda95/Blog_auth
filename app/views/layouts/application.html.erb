<!DOCTYPE html>
<html class="h-full bg-gray-50">
<head>
  <title>BlogAuth</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= javascript_importmap_tags %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
</head>
<body class="h-full">
  <div class="min-h-full">
    <nav class="bg-indigo-600">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div class="flex h-16 items-center justify-between">
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <h1 class="text-white font-bold text-xl">BlogAuth</h1>
            </div>
            <div class="hidden md:block">
              <div class="ml-10 flex items-baseline space-x-4">
                <%= link_to "Home", root_path, class: "text-white hover:bg-indigo-500 hover:bg-opacity-75 px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Posts", posts_path, class: "text-white hover:bg-indigo-500 hover:bg-opacity-75 px-3 py-2 rounded-md text-sm font-medium" %>
              </div>
            </div>
          </div>
          <div class="hidden md:block">
            <div class="ml-4 flex items-center md:ml-6">
              <% if current_user %>
                <span class="text-white mr-4">Bem-vindo, <%= current_user.email %></span>
                <%= link_to "Novo Post", new_post_path, class: "bg-indigo-400 hover:bg-indigo-300 text-white px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Logout", logout_path, method: :delete, data: { turbo_method: :delete, turbo_confirm: "Deseja sair?" }, class: "text-white hover:bg-indigo-500 hover:bg-opacity-75 px-3 py-2 rounded-md text-sm font-medium" %>
              <% else %>
                <%= link_to "Login", login_path, class: "text-white hover:bg-indigo-500 hover:bg-opacity-75 px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Cadastro", signup_path, class: "bg-indigo-400 hover:bg-indigo-300 text-white px-3 py-2 rounded-md text-sm font-medium" %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </nav>
    
    <main>
      <div class="mx-auto max-w-7xl py-6 sm:px-6 lg:px-8">
        <% if notice %>
          <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline"><%= notice %></span>
          </div>
        <% end %>
        <%= yield %>
      </div>
    </main>
  </div>
</body>
</html>